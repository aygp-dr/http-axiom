\* TLC config to demonstrate status-code comparison is insufficient.
\* This SHOULD produce a counterexample trace (property violation).

CONSTANTS
    Methods = {"GET", "PUT", "POST", "DELETE"}
    MaxOps = 4
    ResourceIds = {"r1"}

SPECIFICATION Spec

INVARIANT TypeOK

\* This property asserts "repeated PUT yields same status code."
\* TLC should VIOLATE this, proving status-code comparison gives false negatives.
PROPERTY StatusCodeIdempotency_SHOULD_FAIL
